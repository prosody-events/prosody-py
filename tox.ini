[tox]
envlist = py311
isolated_build = True

[testenv]
deps =
    maturin>=1.6,<2.0
commands =
    maturin develop --extras dev --features admin-client
    pip install -e ".[dev]"
    pytest tests/test_prosody.py
