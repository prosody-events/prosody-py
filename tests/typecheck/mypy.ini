[mypy]
python_version = 3.10
strict = True
# ``Message`` has a PEP 696 default supplied by typing_extensions. Keep the
# fixture usable with older locally-installed mypy releases while assert_type
# checks below continue to guard the concrete consumer-facing types.
disallow_any_generics = False
